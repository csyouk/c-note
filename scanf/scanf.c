#include <stdio.h>

int sth;

int main(void){
  scanf("%d\n", &sth);
  printf("your input is : %d\n", sth);
  // scanf read the data input and saved to text stream and store to the variable
  return 0;
}
